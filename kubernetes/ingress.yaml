apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: banana-ingress

spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: banana-service
                port:
                  number: 80